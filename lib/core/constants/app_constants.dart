import 'dart:ui';
import 'package:flutter/foundation.dart';

/// 앱 전체 상수 정의
class AppConstants {
  AppConstants._();

  /// 앱 이름
  static const String appName = 'My Coloring Book';

  /// 스플래시 화면 지속 시간 (초)
  static const int splashDuration = 2;

  /// 기본 색상 팔레트
  static const List<Color> defaultColors = [
    Color(0xFFE53935), // Red
    Color(0xFFD81B60), // Pink
    Color(0xFF8E24AA), // Purple
    Color(0xFF5E35B1), // Deep Purple
    Color(0xFF3949AB), // Indigo
    Color(0xFF1E88E5), // Blue
    Color(0xFF039BE5), // Light Blue
    Color(0xFF00ACC1), // Cyan
    Color(0xFF00897B), // Teal
    Color(0xFF43A047), // Green
    Color(0xFF7CB342), // Light Green
    Color(0xFFC0CA33), // Lime
    Color(0xFFFDD835), // Yellow
    Color(0xFFFFB300), // Amber
    Color(0xFFFB8C00), // Orange
    Color(0xFFF4511E), // Deep Orange
    Color(0xFF6D4C41), // Brown
    Color(0xFF757575), // Grey
    Color(0xFF546E7A), // Blue Grey
    Color(0xFF000000), // Black
    Color(0xFFFFFFFF), // White
  ];

  // ============================================================
  // 📢 AdMob 광고 ID 설정
  // ============================================================
  // 
  // 🔧 배포 전 체크리스트:
  // 1. AdMob 콘솔에서 앱 등록 (https://admob.google.com)
  // 2. 배너 광고 단위 생성 → ID를 _productionBannerAdUnitId에 입력
  // 3. 보상형 광고 단위 생성 → ID를 _productionRewardedAdUnitId에 입력
  // 4. 릴리즈 빌드로 테스트 후 배포
  //
  // ⚠️ 주의: 테스트용 ID는 디버그 빌드에서만 사용됩니다.
  //         릴리즈 빌드에서는 자동으로 프로덕션 ID가 적용됩니다.
  // ============================================================

  // ──────────────────────────────────────────────────────────────
  // 🚀 프로덕션 광고 ID (여기에 실제 광고 ID를 입력하세요!)
  // ──────────────────────────────────────────────────────────────
  
  /// 프로덕션 배너 광고 ID
  /// AdMob 콘솔에서 생성한 배너 광고 단위 ID를 여기에 입력하세요
  /// 형식: ca-app-pub-XXXXXXXXXXXXXXXX/YYYYYYYYYY
  static const String _productionBannerAdUnitId = 
      'ca-app-pub-XXXXXXXXXXXXXXXX/YYYYYYYYYY'; // ← 여기에 실제 ID 입력
  
  /// 프로덕션 보상형 광고 ID
  /// AdMob 콘솔에서 생성한 보상형 광고 단위 ID를 여기에 입력하세요
  /// 형식: ca-app-pub-XXXXXXXXXXXXXXXX/YYYYYYYYYY
  static const String _productionRewardedAdUnitId = 
      'ca-app-pub-XXXXXXXXXXXXXXXX/YYYYYYYYYY'; // ← 여기에 실제 ID 입력

  // ──────────────────────────────────────────────────────────────
  // 🧪 테스트 광고 ID (수정하지 마세요)
  // ──────────────────────────────────────────────────────────────
  
  /// 테스트용 배너 광고 ID (Google 공식 테스트 ID)
  static const String _testBannerAdUnitId = 
      'ca-app-pub-3940256099942544/6300978111';
  
  /// 테스트용 보상형 광고 ID (Google 공식 테스트 ID)
  static const String _testRewardedAdUnitId = 
      'ca-app-pub-3940256099942544/5224354917';

  // ──────────────────────────────────────────────────────────────
  // 📌 실제 사용되는 광고 ID (자동 선택)
  // ──────────────────────────────────────────────────────────────
  
  /// 배너 광고 ID
  /// - 디버그 빌드: 테스트 ID 사용
  /// - 릴리즈 빌드: 프로덕션 ID 사용
  static String get bannerAdUnitId => 
      kReleaseMode ? _productionBannerAdUnitId : _testBannerAdUnitId;
  
  /// 보상형 광고 ID
  /// - 디버그 빌드: 테스트 ID 사용
  /// - 릴리즈 빌드: 프로덕션 ID 사용
  static String get rewardedAdUnitId => 
      kReleaseMode ? _productionRewardedAdUnitId : _testRewardedAdUnitId;
}
